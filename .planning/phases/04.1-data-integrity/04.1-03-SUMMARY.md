---
phase: 04.1-data-integrity
plan: 03
subsystem: database
tags: [prisma, typescript, data-quality, reporting, observability]

requires:
  - phase: 04.1-01
    provides: silent failure fixes for FRED vintage and OHLCV ingest
  - phase: 04.1-02
    provides: null imputation fixes and benchmark pre-validation

provides:
  - "npm run report:data-quality command"
  - "Per-factor null % by year for FactorFeatureMatrix with WARNING markers"
  - "OhlcvDaily ticker coverage report against full universe list"

affects: [04.2, phase-5-allocation, backtest-engine]

tech-stack:
  added: []
  patterns:
    - "Report scripts always exit 0 — display only, never a CI gate"
    - "Prisma.$queryRaw with Prisma.sql tagged template for aggregation queries"
    - "padEnd/padStart/separator/row helper pattern for CLI table output"

key-files:
  created:
    - scripts/macro-engine/report-data-quality.ts
  modified:
    - package.json

key-decisions:
  - "report:data-quality exits 0 always — visibility tool, not a gate (consistent with report:data)"
  - "WARNING threshold set at >20% null for any single factor dimension per year"
  - "OhlcvDaily coverage uses distinct ticker query + Set comparison against getUniverse() list"

patterns-established:
  - "Null % computed as (total - factorCount) / total * 100 — avoids COUNT(NULL) confusion"
  - "bigint cast via Number() for Postgres COUNT() return type compatibility"

requirements-completed: [DATA-01, DATA-02, FEAT-03]

duration: 8min
completed: 2026-04-08
---

# Phase 4.1 Plan 03: Data Quality Report Summary

**`npm run report:data-quality` script printing per-factor null % by year with WARNING markers and OhlcvDaily coverage check across the full universe ticker list**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-08T00:00:00Z
- **Completed:** 2026-04-08T00:08:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Created `scripts/macro-engine/report-data-quality.ts` (158 lines) with two report sections
- Section A: aggregates FactorFeatureMatrix via raw SQL, prints null % for all 6 z-score factors per year, flags years where any factor exceeds 20% null with `WARNING`
- Section B: queries distinct OhlcvDaily tickers, compares against `getUniverse()` list, prints `present` / `MISSING` per ticker with coverage summary
- Added `report:data-quality` npm script entry to package.json

## Task Commits

1. **Task 1: Create report-data-quality.ts script** - `1ef017c` (feat)
2. **Task 2: Add npm script entry to package.json** - `26cdb88` (chore)

## Files Created/Modified

- `scripts/macro-engine/report-data-quality.ts` - Data quality report script with FactorFeatureMatrix null coverage + OhlcvDaily ticker coverage
- `package.json` - Added `report:data-quality` script entry

## Decisions Made

- Script always exits 0 — consistent with the existing `report:data` pattern; report scripts are observability tools, not CI gates
- WARNING threshold at >20% null per factor per year — matches the plan spec
- Used `bigint` typed interface for Postgres COUNT() return values, cast via `Number()` to avoid arithmetic errors

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Observability layer complete: developers can now run `npm run report:data-quality` to see exactly which years have sparse factor data before running regime classification or backtesting
- All three Wave 3 data integrity plans (04.1-01 through 04.1-03) are complete
- Ready to proceed to Phase 5 (Allocation Signals)

---
*Phase: 04.1-data-integrity*
*Completed: 2026-04-08*
