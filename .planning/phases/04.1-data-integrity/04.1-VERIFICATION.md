---
phase: 04.1-data-integrity
verified: 2026-04-08T00:00:00Z
status: passed
score: 7/7 must-haves verified
---

# Phase 4.1: Data Integrity Verification Report

**Phase Goal:** All model inputs are validated for completeness and correctness before any model runs — silent null imputation replaced with explicit checks, FRED vintage fallbacks flagged, data quality report added, -1.000 max drawdown resolved.
**Verified:** 2026-04-08
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | FRED vintage failures throw — no silent fallback to fetchFredCurrentObservations | VERIFIED | alfred.ts L39-44 and L76-81 throw on HTTP failure and empty-rows edge case; `fetchFredCurrentObservations` exists as dead code only — zero call sites in alfred.ts |
| 2 | maxDrawdown returns `number \| null`, guards for empty/zero series | VERIFIED | metrics.ts L39: signature `number \| null`; L40-43 returns null for empty; L44-47 returns null for all-zero |
| 3 | Dates with >50% null dimensions excluded before clustering | VERIFIED | cluster.ts L60: `if (nullFraction > 0.5) { excludedDates++; continue; }` |
| 4 | Pre-flight guard in regime/index.ts throws if vectors.length < 100 or magnitude < 0.01 | VERIFIED | index.ts L28-33 throws on `vectors.length < 100`; L38-44 throws on `avgVectorMagnitude < 0.01` |
| 5 | Training rows with >3 null dims excluded; SPY price gaps throw before scoring | VERIFIED | backtest/index.ts L169: `if (countNullDimensions(row) > 3) { excludedTrainRows++; continue; }`; L232-241: throws if `missingBenchmarkDates.length > 0` |
| 6 | scripts/macro-engine/report-data-quality.ts exists and shows per-factor null % by year | VERIFIED | File exists at 159 lines; `printFactorNullTable()` runs raw SQL grouping by year, outputs null % per z-score column with WARNING flag for >20% |
| 7 | package.json has report:data-quality script | VERIFIED | `"report:data-quality": "npx tsx scripts/macro-engine/report-data-quality.ts"` at line 23 |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `lib/macro-engine/providers/alfred.ts` | VERIFIED | Throws on HTTP failure and empty-rows case; no call sites for `fetchFredCurrentObservations` remain inside `fetchFredAllVintages` |
| `lib/macro-engine/backtest/metrics.ts` | VERIFIED | `maxDrawdown` signature is `number \| null`; guards for empty array and all-zero array both present |
| `lib/macro-engine/regime/cluster.ts` | VERIFIED | `buildDailyFeatureVectors` excludes dates with `nullFraction > 0.5` with a counter and console.warn |
| `lib/macro-engine/regime/index.ts` | VERIFIED | Pre-flight throws for `vectors.length < 100` and `avgVectorMagnitude < 0.01` |
| `lib/macro-engine/backtest/index.ts` | VERIFIED | Train-row exclusion for `>3` null dims; benchmark gap detection throws with date sample before scoring |
| `scripts/macro-engine/report-data-quality.ts` | VERIFIED | Substantive: SQL query, table rendering, WARNING flag per year, OHLCV coverage table |
| `package.json` (script) | VERIFIED | `report:data-quality` script present |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `fetchFredAllVintages` (alfred.ts) | throw (no fallback) | removed call sites | VERIFIED | `return fetchFredCurrentObservations(...)` call sites eliminated; both error paths now throw |
| `aggregateMetrics` (metrics.ts) | `maxDrawdown` null | `?? 0` coalesce in backtest/index.ts L383 | VERIFIED | DB persist uses `metric.maxDrawdown ?? 0`; console log uses `?.toFixed(3) ?? 'null'` |
| `buildDailyFeatureVectors` (cluster.ts) | `classifyRegimes` (index.ts) | called at index.ts L25 | VERIFIED | Pre-flight checks operate on the returned vectors immediately after the call |
| `countNullDimensions` (backtest/index.ts) | train-row exclusion loop | L169 inside for-loop | VERIFIED | Function defined at L40-42; invoked at L169 |
| `benchmarkReturnMap` (backtest/index.ts) | SPY gap throw | L233-241 | VERIFIED | Missing dates computed, throws with sample dates if any missing |

---

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| DATA-01 | FRED vintage fallback eliminated (look-ahead bias) | SATISFIED | alfred.ts throws on failure; no call to `fetchFredCurrentObservations` inside `fetchFredAllVintages` |
| DATA-02 | Null-heavy rows excluded before model training | SATISFIED | cluster.ts >50% null date exclusion; backtest/index.ts >3 null dims train-row exclusion |
| FEAT-01 | Data quality report with per-factor null % by year | SATISFIED | report-data-quality.ts + `report:data-quality` npm script |
| FEAT-03 | -1.000 max drawdown resolved (empty/zero series guard) | SATISFIED | metrics.ts returns `null` for empty and all-zero series; callers handle null correctly |

---

### Anti-Patterns Found

None. No TODO/FIXME/placeholder comments or stub implementations found in the verified files.

Notable observation: `fetchFredCurrentObservations` remains as dead code in alfred.ts (the plan explicitly preserved the function definition, only removing the call sites). This is intentional per the plan — not a gap.

---

### Human Verification Required

None. All must-haves are verifiable through static analysis.

---

### Summary

All 7 must-haves are fully implemented and wired. The phase goal is achieved:

- FRED vintage failures now throw hard errors at both the HTTP failure path and the empty-parse path, with no silent fallback to current (non-point-in-time) observations.
- `maxDrawdown` returns `number | null` with explicit guards, resolving the -1.000 artifact. The null is handled correctly at the persistence site (`?? 0`) and the log site (`?? 'null'`).
- Cluster building excludes dates where more than 50% of dimension values are null, preventing poor-quality dates from distorting regime centroids.
- Regime classification refuses to run on fewer than 100 vectors or near-zero average magnitude, making data insufficiency an explicit failure rather than silent bad output.
- Backtest training excludes rows with more than 3 null dimensions, and throws before scoring if SPY benchmark prices are missing for any test date.
- The data quality report script exists, queries the database for per-factor null percentages by year with WARNING flags, and is registered as an npm script.

---

_Verified: 2026-04-08_
_Verifier: Claude (gsd-verifier)_
